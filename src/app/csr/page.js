import React from "react";
import CSRPageComponent from "./CSRPageComponent";

export const metadata = {
	title: "CSR",
};

const page = () => {
	return (
		<div>
			<CSRPageComponent />
		</div>
	);
};

export default page;
